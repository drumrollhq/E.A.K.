.dialogue
  display block
  width 410px
  margin-left -205px
  margin-top 30px
  display none
  padding arem
  color white

  position absolute
  top 0
  left 50%

  @media (max-width: 400px)
    width 100%
    margin-left 0
    left 0

  &.active
    display block
    animation dialogue-in 0.2s ease-out

  &.disabled
    display block
    visibility hidden
    animation dialogue-out 0.2s ease-in

  h1, h2
    text-align center
    font-family brandFont
    font-size 36px
    line-height 1.5em

@keyframes dialogue-in
  from
    transform scale(0.8) translateZ(0)
    opacity 0

  to
    transform scale(1) translateZ(0)
    opacity 1

@keyframes dialogue-out
  from
    transform scale(1) translateZ(0)
    opacity 1
    visibility visible

  to
    transform scale(1.2) translateZ(0)
    opacity 0
    visibility hidden
