language: node_js
node_js:
  - "0.10"

before_script:
  - "sudo add-apt-repository -y ppa:jon-severinsson/ffmpeg"
  - "sudo apt-get update"
  - "sudo apt-get install ffmpeg"
  - "npm install -g bower"
  - "bower install"
  - "gulp build -o"
